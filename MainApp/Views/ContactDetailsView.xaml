<UserControl x:Class="MainApp.Views.ContactDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainApp.Views"
             xmlns:viewmodels="clr-namespace:MainApp.ViewModels"
             xmlns:models="clr-namespace:Business.Models;assembly=Business"
             d:DataContext="{d:DesignInstance Type=viewmodels:ContactDetailsViewModel}"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="550" d:DesignWidth="350">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Contact details" FontSize="20" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Margin="0 20 0 0">
            <TextBlock Text="First Name"/>
            <TextBlock Text="{Binding ContactModel.FirstName}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Last Name"/>
            <TextBlock Text="{Binding ContactModel.LastName}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Email"/>
            <TextBlock Text="{Binding ContactModel.Email}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Phone number"/>
            <TextBlock Text="{Binding ContactModel.PhoneNumber}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Address"/>
            <TextBlock Text="{Binding ContactModel.Address}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Zip code"/>
            <TextBlock Text="{Binding ContactModel.ZipCode}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="City"/>
            <TextBlock Text="{Binding ContactModel.City}" Margin="0,0,0,10" Padding="4" />
            
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Button Content="Edit" Padding="5" Margin="0 10 0 5" Command="{Binding GoToEditViewCommand }"/>
            <Button Content="Cancel" Padding="5" Margin="0 10 0 5" Command="{Binding CancelCommand }"/>
            
        </StackPanel>

    </Grid>
</UserControl>
