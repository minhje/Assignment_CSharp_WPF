<UserControl x:Class="MainApp.Views.ContactAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainApp.Views"
             xmlns:viewmodels="clr-namespace:MainApp.ViewModels"
             xmlns:models="clr-namespace:Business.Models;assembly=Business"
             d:DataContext="{d:DesignInstance Type=viewmodels:ContactAddViewModel}"
             
             mc:Ignorable="d" 
             
             Background="White"
             d:DesignHeight="450" d:DesignWidth="300">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="New Contact" FontSize="20" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Margin="0 20 0 0">
            <TextBlock Text="First Name"/>
            <TextBox Text="{Binding Contact.FirstName}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Last Name"/>
            <TextBox Text="{Binding Contact.LastName}" Margin="0,0,0,10" Padding="4" />
            
            <TextBlock Text="Email"/>
            <TextBox Text="{Binding Contact.Email}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Phone number"/>
            <TextBox Text="{Binding Contact.PhoneNumber}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="Address"/>
            <TextBox Text="{Binding Contact.Address}" Margin="0,0,0,10" Padding="4" />
            
            <TextBlock Text="Zip code"/>
            <TextBox Text="{Binding Contact.ZipCode}" Margin="0,0,0,10" Padding="4" />

            <TextBlock Text="City"/>
            <TextBox Text="{Binding Contact.City}" Margin="0,0,0,10" Padding="4" />

        </StackPanel>

        <StackPanel Grid.Row="2">
            <Button Content="Save" Padding="5" Margin="0 10 0 5" Command="{Binding SaveCommand }"/>
            <Button Content="Cancel" Padding="5" Margin="0 10 0 5" Command="{Binding CancelCommand}"/>
        </StackPanel>

    </Grid>
</UserControl>
